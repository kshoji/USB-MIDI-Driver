language: android
jdk: openjdk7
android:
  components:
    - build-tools-21.1.2
    - extra-android-m2repository
    - android-21
  licenses:
    - '.+'
before_install:
 - chmod +x gradlew
script:
  - ./gradlew check
  - cd ./MIDIDriver && mvn install -DskipTests=true

